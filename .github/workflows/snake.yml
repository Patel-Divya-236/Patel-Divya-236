name: Generate snake animation

on:
  # Twice a day, so the snake keeps up with new contributions.
  schedule:
    - cron: "0 */12 * * *"
  # Lets you run it by hand from the Actions tab — needed for the first run,
  # since the README points at files that do not exist until this succeeds.
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      # Required so the action can push the generated SVGs to the output branch.
      contents: write

    steps:
      - name: Generate the snake SVGs
        uses: Platane/snk@v3
        id: snake-gif
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-snake.svg
            dist/github-snake-dark.svg?palette=github-dark

      - name: Push them to the output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
